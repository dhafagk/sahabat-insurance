services:
  payload:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:3969:3000"
    volumes:
      - ./public/media:/app/public/media
    env_file:
      - .env.docker
